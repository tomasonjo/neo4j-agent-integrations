# Neo4j AgentForce Bridge — Python Dependencies

# Core
neo4j>=6.0.0                 # Neo4j Python driver v6.x (Jan 2026) — use 'neo4j' pkg, NOT 'neo4j-driver'
fastapi>=0.129.0             # REST API framework (auto-generates OpenAPI 3.0 spec)
uvicorn[standard]>=0.29.0    # ASGI server

# AgentForce Client
requests>=2.31.0             # HTTP client for Agent API
salesforce-agentforce>=0.1.0 # Official Salesforce AgentForce Python SDK (optional)
sseclient-py>=1.8.0          # For streaming SSE responses from Agent API

# Embeddings (for vector search tool)
openai>=1.35.0               # OpenAI embeddings (text-embedding-3-small)

# GraphRAG (for graphrag_bridge_server.py)
neo4j-graphrag>=1.13.0       # Neo4j GraphRAG Python library (Feb 3 2026)
                             # Provides: VectorRetriever, VectorCypherRetriever,
                             #           HybridRetriever, HybridCypherRetriever,
                             #           GraphRAG pipeline class

# Production extras
python-dotenv>=1.0.0         # Load .env files
pydantic>=2.0.0              # Data validation (included with FastAPI)
httpx>=0.27.0                # Async HTTP (optional, for async Neo4j calls)
